server {
  listen 80;

  server_name notes.localhost.com;
  proxy_buffering off;
  client_max_body_size 10m;

  location / {
    proxy_pass http://localhost:5173;
  }

  location /trpc/ {
    proxy_pass http://localhost:4000/;
  }
}