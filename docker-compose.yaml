services:
  db:
    image: postgres:16.6
    restart: always
    environment:
      POSTGRES_PASSWORD: postgres
    ports:
      - '3000:5432'
    volumes:
      - ./.docker/postgresql:/var/lib/postgresql/data